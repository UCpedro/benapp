# BenBenefits (SwiftUI + MVVM)

Proyecto iOS listo para abrir en Xcode:

1. Abre `BenBenefits.xcodeproj`.
2. Selecciona el scheme **BenBenefits**.
3. Elige un simulador iPhone.
4. Run (`⌘R`).

La app usa datos mock locales y guarda tarjetas seleccionadas en `UserDefaults`.
